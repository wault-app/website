export type AccountItemProps = {
    platform: string;
    username?: string;
    password?: string;
} | {
    loading: true;
};

const AccountItem = (props: AccountItemProps) => {
    return (

    );
};

export default AccountItem;